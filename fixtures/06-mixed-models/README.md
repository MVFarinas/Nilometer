# 06-mixed-models: A mixed-model session

**Setup:** One session uses Sonnet, Opus, and Haiku.

**Proves:** Tokens are attributed per request to each model, never to one session-level model.

**Rules:** D-007; skill fixture 6. Expected values in `expected.json` were computed by hand from the setup above.
