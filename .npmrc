# No dependency runs code at install time. better-sqlite3 ships prebuilt binaries, so nothing
# here needs a build step, and the dev dependencies' install scripts aren't worth the risk.
ignore-scripts=true
